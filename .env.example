# Since the ".env" file is gitignored, you can use the ".env.example" file to
# build a new ".env" file when you clone the repo. Keep this file up-to-date
# when you add new variables to `.env`.

# This file will be committed to version control, so make sure not to have any
# secrets in it. If you are cloning this repo, create a copy of this file named
# ".env" and populate it with your secrets.

# When adding additional environment variables, the schema in "/src/env.js"
# should be updated accordingly.

NODE_ENV="development"
NEXT_PUBLIC_ENVIRONMENT="development"
DEFAULT_LOCALE="en"

# Prisma
# https://www.prisma.io/docs/reference/database-reference/connection-urls#env
DATABASE_URL="file:./data/db.sqlite"

# Next Auth
# You can generate a new secret on the command line with:
# openssl rand -base64 32
BETTER_AUTH_SECRET=

BETTER_AUTH_URL=http://localhost:3000 # Base URL of your app

# Next Auth Discord Provider
AUTH_AUTHENTIK_LABEL=""
AUTH_AUTHENTIK_ID=""
AUTH_AUTHENTIK_SECRET=""
AUTH_AUTHENTIK_ISSUER=""

# Email configuration for magic links
# SMTP server settings
EMAIL_SERVER_HOST="smtp.gmail.com"
EMAIL_SERVER_PORT="587"
EMAIL_SERVER_USER="your-email@gmail.com"
EMAIL_SERVER_PASSWORD="your-app-password"
EMAIL_FROM="your-email@gmail.com"

# Registration settings
# Set to "false" to disable new user registration via email
MAIL_REGISTRATION_ENABLED="true"

# Public content access control
# Set to "true" to require authentication for listening to public audios and playlists
# When enabled, users must be logged in to access public audio/playlist listen pages
# When disabled (default), anyone can listen to public audios and playlists without logging in
REQUIRE_AUTH_FOR_PUBLIC_CONTENT="false"

NEXT_PUBLIC_SENTRY_DSN="https://12345@errors.example.com/123"
NEXT_PUBLIC_SENTRY_LOG_LEVELS="log,warn,error"

#SENTRY_AUTH_TOKEN="" # only ncessary for build process (can also be placed in .env.sentry-build-plugin) 